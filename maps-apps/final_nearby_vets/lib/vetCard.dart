import 'package:final_nearby_vets/models/vet_clinic.dart';
import 'package:flutter/material.dart';

class VetCard extends StatelessWidget {

  VetClinic vetClinic;

  VetCard({required this.vetClinic});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vet List"),
      ),
      body: Column(
        children: [
          Card(
          child: ,
      );
        ],
      ),
    );
  }
}
