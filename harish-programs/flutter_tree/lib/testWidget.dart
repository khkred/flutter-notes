import 'package:flutter/material.dart';

class TestWidget extends StatelessWidget {

  List<Map<String,Object>> sampleList;

  TestWidget(this.sampleList);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
