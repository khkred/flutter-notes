import 'package:flutter/material.dart';

class VetsList extends StatelessWidget {
  const VetsList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Vets List'),
    );
  }
}
